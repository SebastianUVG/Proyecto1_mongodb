services:
  api:
    build:
      context: ./python_backend
      dockerfile: Dockerfile
    env_file:
      - ./python_backend/.env
    environment:
      ENFORCE_NO_COLLSCAN: ${ENFORCE_NO_COLLSCAN:-0}
    ports:
      - "8000:8000"
    restart: unless-stopped